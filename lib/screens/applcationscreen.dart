import 'package:flutter/material.dart';

class Application_Screen extends StatelessWidget {
  const Application_Screen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Text("data"),
      ),
    );
  }
}
